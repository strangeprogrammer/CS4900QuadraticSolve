Notes to self: (DMD)

Compiling t1 will not work for some reason - count init()
I believe the mock_quadratic object has been implemented correctly
t1 file needs work
makefile probably needs some work as well
